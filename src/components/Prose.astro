<div class="prose"><slot /></div>

<style is:global>
  .prose { font-size: var(--text-body); line-height: 1.65; letter-spacing: 0.01em; }
  .prose strong { font-weight: 600; }
  .prose :where(h2, h3) { scroll-margin-top: calc(var(--header-h) + 1.25rem); }
  .prose h2 { margin-block: 3rem 0.9rem; font-size: 1.5rem; font-weight: 600; letter-spacing: -0.02em; line-height: 1.25; }
  .prose h3 { margin-block: 2.25rem 0.7rem; font-size: 1.2rem; font-weight: 600; line-height: 1.3; }
  .prose p, .prose ul, .prose ol, .prose blockquote, .prose pre { margin-block: 0 1.5rem; }
  .prose :where(ul, ol) { padding-inline-start: 1.4rem; }
  .prose li + li { margin-top: 0.4rem; }
  .prose li > :last-child { margin-bottom: 0; }
  .prose a { text-decoration: underline; text-decoration-color: var(--line-strong); text-underline-offset: 0.18em; }
  .prose :where(a, code) { overflow-wrap: anywhere; }
  .prose blockquote { margin-inline: 1.25rem; padding-block: 0.25rem; color: var(--muted); }
  .prose code { padding: 0.12em 0.3em; border-radius: 0.25rem; background: var(--surface); font-family: var(--font-mono); font-size: 0.88em; }
  .prose pre { overflow-x: auto; padding-block: var(--space-inset); border-radius: var(--radius); background: #1d1710; color: #fff8ea; font-family: var(--font-mono); font-size: 0.875rem; line-height: 1.7; }
  .prose pre code { display: grid; min-width: 100%; width: max-content; padding: 0; background: transparent; color: inherit; }
  .prose pre .line { padding-inline: var(--space-inset); }
  .prose pre .line.highlighted { background: color-mix(in srgb, #f6c667 14%, transparent); }
  .prose img { height: auto; margin-block: 2rem; border-radius: var(--radius); }
  .prose hr { margin-block: 3rem; border: 0; border-top: 1px solid var(--line); }
  .prose table { display: block; width: 100%; margin-block: 2rem; overflow-x: auto; border-collapse: collapse; font-variant-numeric: lining-nums tabular-nums; }
  .prose :where(th, td) { padding: 0.65rem 0.85rem; border-bottom: 1px solid var(--line); text-align: left; }
  .prose th { font-size: var(--text-sm); font-weight: 600; }
  .prose details:not(.mdx-demo) { margin-block: 2rem; padding: var(--space-inset); border-radius: var(--radius); background: var(--surface); }
  .prose summary { cursor: pointer; font-weight: 600; }
  .prose details:not(.mdx-demo) > :last-child { margin-bottom: 0; }
  .prose kbd { padding: 0.1em 0.35em; border: 1px solid var(--line-strong); border-radius: 0.25rem; font-family: var(--font-mono); font-size: 0.82em; }
  .prose .katex-display { margin-block: 1rem; padding: 0.5rem; overflow-x: auto; overflow-y: hidden; }
  .prose .katex { font-size: 1.08em; letter-spacing: 0; text-autospace: no-autospace; }
  .prose .katex svg { max-width: none; }
  .prose .footnotes { margin-block: 2rem 0; font-size: var(--text-sm); color: var(--muted); }
  .prose .footnotes li { margin-block: 0.15rem; }
  .prose .footnotes :is(h2, h3, hr) { display: none; }
  .prose sup a { text-decoration: none; }

  @supports (corner-shape: squircle) {
    .prose :is(pre, img, details:not(.mdx-demo)) { corner-shape: squircle; }
  }
</style>
